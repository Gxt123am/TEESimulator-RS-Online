# OmegaRelay provider daemon config.
# Copy to provider.conf and edit before running.

url = ws://127.0.0.1:18443/
psk = smoke-test-psk-1234567890abcdef-must-be-strong
device_id = device-b-1
tls_insecure = false
